// DISABLED - This file has been disabled to prevent conflicts with external script suppression
// All error suppression is now handled by /public/suppress-errors.js

console.log(
  "⚠️ simpleErrorSuppression.ts is disabled - using external script instead",
);

// Export empty object to prevent import errors
export const simpleErrorSuppression = {};
